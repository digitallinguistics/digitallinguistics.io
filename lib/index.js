const externalRedirect = require(`./redirect`);
const feather          = require(`./feather`);
const getLocals        = require(`./locals`);

module.exports = {
  externalRedirect,
  feather,
  getLocals,
};
